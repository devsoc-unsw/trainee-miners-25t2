{
    "buildCommand": "pnpm build:web",
    "outputDirectory": ".next",
    "framework": "nextjs",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
    "functions": {
        "src/app/api/**": {
            "runtime": "nodejs20.x"
        }
    }
}